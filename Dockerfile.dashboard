FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir textual httpx

COPY dashboard.py .

EXPOSE 8080

CMD ["python", "-m", "textual", "serve", "--host", "0.0.0.0", "--port", "8080", "dashboard.py"]
