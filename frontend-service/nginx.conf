events {}

http {
    server {
        listen 80;
        root /usr/share/nginx/html;

        location = /app {
            try_files /app.html =404;
        }

        location / {
            try_files $uri /index.html;
        }
    }
}
